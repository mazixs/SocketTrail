# Изменения

## Не выпущено

- Windows 10/11: портативный zip (`make win`). Сокеты через IP Helper с PID владельца,
  процессы через Toolhelp32, PTR и ASN через `DnsQuery_W`. Иконка, сведения о версии
  и манифест в exe.
- Windows без Wireshark и Npcap: домены из DNS-кеша системы без прав администратора,
  а с правами - свой захват пакетов через встроенный PktMon и ETW (домены из DNS и SNI,
  трафик, дампы .pcapng). Кнопка перезапуска от администратора прямо в окне,
  окно при этом не закрывается. dumpcap остается запасным путем.
- Имя из DNS заменяет ранее показанное PTR-имя, если пришло позже.
- Закрытие окна отслеживается по SSE-каналу `/api/alive` на всех ОС.
- `-i` принимает несколько интерфейсов через запятую.
- TIME_WAIT-сокеты без истории больше не попадают в список: десятки тысяч
  локальных TIME_WAIT вытесняли живые соединения, и у большинства пропадал PID.
- CI: fmt, clippy, тесты на Linux, под Wine и на Windows; релиз по тегу собирает
  deb и zip, SHA256SUMS и attestation.
- Окно на Chromium-браузере со своим профилем и классом окна: не смешивается с
  обычным Chrome, своя иконка в панели задач. Поиск Chrome, Chromium, Edge, Brave,
  Vivaldi, включая snap-сборки.
- Закрытие окна, Ctrl+C и SIGTERM завершают программу аккуратно: дамп дописывается
  и получает `.json`-карту, кеш имен сохраняется, dumpcap останавливается.
- Защита локального API: проверка `Host` (DNS rebinding) и `Origin` для POST (CSRF).
  Раньше любая открытая в браузере страница могла очистить историю или запустить дамп.
- Живой разбор и дамп без фильтра по процессу больше не захватывают loopback,
  кроме DNS. При активном локальном трафике это снижает нагрузку в разы
  (замер: 34% + 32% CPU у dumpcap -> 3% + 0%) и не дает дампу расти на гигабайты.
- Иконка в доке на Wayland: окно открывается по `/sockettrail`, его app_id
  прописан в ярлыке, плюс скрытые ярлыки для других Chromium-браузеров.
- Пакет .deb: `make deb`.
- Окно отслеживается по главному процессу браузера с профилем SocketTrail: Chrome,
  запущенный из меню GNOME, уходит в свой systemd-scope, и исходный PID выходит
  через 2 секунды - раньше программа принимала это за закрытие окна.
- Каталоги вынесены в `src/paths.rs`, запуск окна - в `src/window.rs`, с заделом под Windows.

## v0.1.0 - 2026-09-20

Первый релиз. Linux.

- Дерево процессов с автообновлением, распознавание Proton и Wine, выбор
  процесса вместе со всеми потомками.
- История соединений по процессу: домен, IP, порт, протокол, состояние,
  владелец сети (ASN), объем трафика. Закрытые соединения остаются в списке.
- Два источника данных: опрос `/proc/net/{tcp,tcp6,udp,udp6}` каждые 200 мс и
  разбор потока pcapng от `dumpcap`. Второй ловит соединения короче интервала
  опроса и дает домены из DNS-ответов и SNI.
- Порты только настоящие: из таблицы сокетов или из заголовка пакета, без
  подстановок по типу службы.
- Имена адресов из четырех источников (SNI, DNS, PTR через resolvectl, подписи
  служебных адресов) с дисковым кешем `~/.cache/sockettrail/names.json`.
- Запись дампа в `~/SocketTrail/<процесс>-<дата>.pcapng` с картой соединений
  в `.json` рядом, по желанию с BPF-фильтром по адресам процесса.
- Автономный HTML-отчет, сводка по адресам, копирование строки и таблицы,
  изменяемые колонки, темная и светлая темы.
- Установка без root: `./install.sh` кладет бинарь в `~/.local/bin` и ярлык в
  меню приложений; фоновый сбор через user-юнит systemd.
